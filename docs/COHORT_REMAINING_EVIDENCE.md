# 尚缺的前端歷史證據

這張表避免把保存JSON可讀取／複製誤寫成原始資料已重建。

| 主線 | 已有證據 | 尚缺與影響 |
|---|---|---|
| KH原始Excel | 33個來源路徑、25組內容、27個病例標籤；只有24個標籤覆蓋現存33例 | 9例在現有解壓縮檔與已索引的15個壓縮檔Excel成員中無同標籤檔名候選（不排除另名檔案）；其餘也需source hash、formatter回覆、日期窗與修訂紀錄才可宣稱完整源頭鏈 |
| KH保存前端→上游 | 0728兩日33例297份相同；以case＋specimen找到唯一global映射 | 只能證明這個保存輸入邊界，不補造當年模型生成 |
| 兩院臨床 | master抽取對到48例clinical432份完全相同 | T／V master54→交付48的完整排除原因仍待確認 |
| 兩院mNGS | 48份保存grouped與上游相同，42份有檢體內容、6份空 | 簡單master的42份nonempty grouped不同；較完整資料生成recipe尚未完整重建 |
| 兩院subset | 保存root27、交付25、比較11均有清單；交付summary實際20＋5來源 | 不以subset數字猜選取關係，不用gold重造篩選 |
| Table 1 | 候選baseline內容可重現 | 歷史病人身分與唯一baseline来源未定，不自動併入KH或兩院 |

論文正式run、模型／文獻與交付的其他界線仍在 [PUBLICATION.md](PUBLICATION.md)。
